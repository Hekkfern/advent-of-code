//**********************************************************************
// GENERATED CODE - DO NOT MODIFY
//**********************************************************************

#include "../sources/solutions.hpp"
#define CATCH_CONFIG_MAIN
#include <catch.hpp>
#include <utils/FileUtils.hpp>

TEST_CASE(
    "[{{YEAR}}_{{DAY}}] Use example input for Part 1",
    "[{{YEAR}}, {{YEAR}}_{{DAY}}, part1, {{YEAR}}_{{DAY}}_part1]")
{
    constexpr auto InputFile{ "input_test.txt" };

    std::string mySolution = aoc_{{YEAR}}_{{DAY}}::solvePart1(InputFile);
    std::string expectedSolution = utils::FileUtils::readFirstLine(
                                       "solution1_test.txt")
                                       .value();

    REQUIRE(mySolution == expectedSolution);
}

TEST_CASE(
    "[{{YEAR}}_{{DAY}}] Use example input for Part 2",
    "[{{YEAR}}, {{YEAR}}_{{DAY}}, part2, {{YEAR}}_{{DAY}}_part2]")
{
    constexpr auto InputFile{ "input_test.txt" };

    std::string mySolution = aoc_{{YEAR}}_{{DAY}}::solvePart2(InputFile);
    std::string expectedSolution = utils::FileUtils::readFirstLine(
                                       "solution2_test.txt")
                                       .value();

    REQUIRE(mySolution == expectedSolution);
}

TEST_CASE(
    "[{{YEAR}}_{{DAY}}] Use real input for Part 1",
    "[{{YEAR}}, {{YEAR}}_{{DAY}}, part1, {{YEAR}}_{{DAY}}_part1]")
{
    constexpr auto InputFile{ "input.txt" };

    std::string mySolution = aoc_{{YEAR}}_{{DAY}}::solvePart1(InputFile);
    std::string expectedSolution = utils::FileUtils::readFirstLine(
                                       "solution1.txt")
                                       .value();

    REQUIRE(mySolution == expectedSolution);
}

TEST_CASE(
    "[{{YEAR}}_{{DAY}}] Use real input for Part 2",
    "[{{YEAR}}, {{YEAR}}_{{DAY}}, part1, {{YEAR}}_{{DAY}}_part2]")
{
    constexpr auto InputFile{ "input.txt" };

    std::string mySolution = aoc_{{YEAR}}_{{DAY}}::solvePart2(InputFile);
    std::string expectedSolution = utils::FileUtils::readFirstLine(
                                       "solution2.txt")
                                       .value();

    REQUIRE(mySolution == expectedSolution);
}
