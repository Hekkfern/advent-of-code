#pragma once

#include <string_view>
#include <chrono>

namespace utils::AocUtils {

void printPart1Solution(std::string_view solution, std::chrono::nanoseconds execTime);
void printPart2Solution(std::string_view solution, std::chrono::nanoseconds execTime);

} // namespace utils::AocUtils
